# NetBox Yandex Cloud Sync

Профессиональная утилита для синхронизации данных между Yandex Cloud и NetBox. Автоматически создает и обновляет записи о виртуальных машинах, дисках и сетевых ресурсах в NetBox на основе актуальных данных из Yandex Cloud.

## ✨ Ключевые особенности

### 🏗️ Автоматическая инфраструктура
- Создание полной иерархии в NetBox: облако → папка → кластер → ВМ
- Автоматическое создание сетевых префиксов на основе VPC и подсетей
- Управление группами кластеров и типами кластеров

### 🖥️ Синхронизация виртуальных машин
- **Статус**: active/offline в зависимости от состояния в Yandex Cloud
- **Ресурсы**: vCPUs, RAM (в MB), суммарный размер дисков
- **Диски**: Создание виртуальных дисков в NetBox с типом и размером
- **Сети**: Интерфейсы с правильными масками подсетей из Yandex Cloud
- **IP адреса**: Включая NAT адреса с автоматическим определением подсетей

### 🔧 Продвинутые возможности
- **Dry-run режим**: Предварительный просмотр всех изменений
- **Автоматическое исправление**: Дублирующиеся IP адреса и отсутствующие primary IP
- **Умная обработка IP**: Проверка существующих адресов и правильные CIDR маски
- **Обработка ошибок**: Продолжение синхронизации даже при частичных сбоях
- **Конфигурация**: Гибкая настройка через переменные окружения
- **Логирование**: Подробные логи с настраиваемым уровнем детализации
- **Валидация**: Встроенные тесты для проверки конфигурации

## 📋 Требования

- **Python**: 3.8 или новее
- **NetBox**: 3.0+ (виртуальные диски требуют NetBox 3.4+)
- **Yandex Cloud**: Доступ к API и действующий IAM токен
- **Сеть**: Доступ к API endpoints Yandex Cloud и NetBox

## 🚀 Быстрый старт

### 1. Установка
```bash
git clone https://github.com/your-username/netbox-yc-sync.git
cd netbox-yc-sync

# Установка зависимостей
make install
# или
pip install -r requirements.txt
```

### 2. Настройка
```bash
# Создание конфигурации из шаблона
cp .env.example .env

# Редактирование конфигурации
nano .env
```

### 3. Конфигурация переменных окружения
```env
# Yandex Cloud
YC_TOKEN=your_yandex_cloud_token_here

# NetBox
NETBOX_URL=https://your-netbox-instance.com
NETBOX_TOKEN=your_netbox_api_token_here
NETBOX_SITE=Yandex Cloud RU

# Опционально
LOG_LEVEL=INFO
```

### 4. Проверка настройки
```bash
# Проверка конфигурации и подключений
make test
```

## 💻 Использование

### Базовые команды
```bash
# Предварительный просмотр изменений
make dry-run
# или
python main.py --dry-run

# Выполнение синхронизации
make run
# или
python main.py

# Синхронизация с отладочными логами
python main.py --log-level DEBUG
```

### Docker
```bash
# Сборка образа
make docker-build

# Запуск в контейнере
make docker-run

# Dry-run в контейнере
make docker-dry
```

### Продвинутые опции
```bash
# Проверка конфигурации
make check-config

# Валидационные тесты
make test

# Очистка временных файлов
make clean
```

## 📁 Структура проекта

```
netbox-yc-sync/
├── clients/                    # 🔌 API клиенты
│   ├── __init__.py
│   ├── netbox_client.py       # NetBox API с автоисправлениями
│   └── yandex_cloud_client.py # Yandex Cloud API клиент
├── sync/                      # ⚙️ Логика синхронизации
│   ├── __init__.py
│   └── synchronizer.py        # Планирование и сравнение
├── config.py                  # 🔧 Управление конфигурацией
├── main.py                    # 🚀 Основной скрипт синхронизации
├── quick_check.py            # 🔍 Быстрая проверка системы
├── Makefile                   # 🛠️ Команды автоматизации
├── requirements.txt           # 📦 Зависимости Python
├── Dockerfile                 # 🐳 Конфигурация контейнера
├── .env.example              # 📋 Шаблон конфигурации
└── README.md                 # 📖 Документация
```

## 📊 Логирование и мониторинг

### Уровни логирования
- **DEBUG**: Детальная информация для отладки
- **INFO**: Стандартная информация о процессе синхронизации
- **WARNING**: Предупреждения о потенциальных проблемах
- **ERROR**: Ошибки, которые не прерывают работу

### Что логируется
- 📡 Подключения к API Yandex Cloud и NetBox
- 📈 Статистика найденных ресурсов (ВМ, сети, диски)
- 📋 Планируемые действия с детализацией
- ✅ Успешные операции создания/обновления
- ⚠️ Предупреждения о существующих ресурсах
- ❌ Ошибки с продолжением работы
- 🔍 Dry-run операции с префиксом [DRY-RUN]

### Пример вывода
```
2024-01-15 10:30:15 - main - INFO - Starting NetBox-YC sync (dry_run=True)
2024-01-15 10:30:16 - clients.yandex_cloud_client - INFO - Fetched Yandex Cloud data: 1 clouds, 30 folders, 6 VPCs, 40 subnets, 220 VMs
2024-01-15 10:30:17 - clients.netbox_client - INFO - Fixed 10 duplicate IP addresses
2024-01-15 10:30:18 - sync.synchronizer - INFO - Total planned actions: 0 (exact: 220, name-only: 0, create: 0)
2024-01-15 10:30:18 - clients.netbox_client - INFO - Assigned primary IPs to 5 VMs
2024-01-15 10:30:18 - main - INFO - Synchronization completed successfully
```

## 🔧 Разработка

### Настройка среды разработки
```bash
# Клонирование и настройка
git clone https://github.com/your-username/netbox-yc-sync.git
cd netbox-yc-sync
make dev-setup

# Редактирование .env файла
nano .env
```

### Тестирование
```bash
# Проверка синтаксиса
make lint

# Быстрая проверка системы
make check

# Проверка конфигурации
make check-config
```

### Workflow
```bash
# Создание новой ветки
git checkout -b feature/new-feature

# Разработка и тестирование
make test
make lint

# Коммит изменений
git add .
git commit -m "feat: добавлена новая функциональность"

# Отправка в репозиторий
git push origin feature/new-feature
```

## 🆘 Поддержка и устранение неполадок

### Частые проблемы
1. **Ошибка подключения к API**: Проверьте токены и сетевую доступность
2. **IP адреса уже существуют**: Утилита автоматически обновляет назначения
3. **Отсутствуют виртуальные диски**: Требуется NetBox 3.4+

### Автоматические исправления
Система автоматически исправляет:
- **Дублирующиеся IP адреса** с разными CIDR масками
- **Отсутствующие primary IP** для виртуальных машин
- **Неправильные назначения IP** к интерфейсам
- **Устаревшие параметры ресурсов** VM

### Получение помощи
- 🐛 **Issues**: Создайте issue в GitHub репозитории
- 📧 **Email**: Свяжитесь с командой разработки
- 💬 **Discussions**: Обсуждения в GitHub Discussions

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 🤝 Благодарности

- Команде Yandex Cloud за отличное API
- Сообществу NetBox за мощную платформу IPAM/DCIM
- Всем контрибьюторам проекта